# Copy this file to .env.production and replace every placeholder before exposing Abra.
ABRA_API_KEYS=replace-with-generated-token
ABRA_APPROVAL_MODE=enforce

# Local defaults use Qwen/Qwen3-Embedding-0.6B and Qwen/Qwen3-Reranker-0.6B
# served by local OpenAI-compatible endpoints. Set EMBEDDING_PROVIDER=compatible
# plus the provider fields below to replace the local defaults with any custom
# embedding provider.
EMBEDDING_PROVIDER=local
EMBEDDING_BASE_URL=http://host.docker.internal:8080/v1
EMBEDDING_API_KEY=
EMBEDDING_MODEL=text-embeddings-inference
EMBEDDING_DIMENSIONS=1024
RERANKER_PROVIDER=local
RERANKER_BASE_URL=http://host.docker.internal:8081
RERANKER_API_KEY=
RERANKER_MODEL=text-embeddings-inference
ALLOW_LOCAL_EMBEDDINGS_IN_PRODUCTION=false

REDACT_PII=true
RATE_LIMIT_MAX=120
RATE_LIMIT_WINDOW=1 minute
ABRA_PORT=18080

# Optional: use managed Postgres instead of the Compose Postgres service.
# ABRA_DATABASE_URL=postgres://user:password@host:5432/abra

# Optional OpenTelemetry tracing over OTLP HTTP.
# OTEL_EXPORTER_OTLP_ENDPOINT=http://otel-collector:4318
# ABRA_TRACING_SAMPLE_RATIO=0.25
# ABRA_TRACING_INSECURE=false
# ABRA_SERVICE_NAME=abra
# ABRA_DEPLOYMENT_ENVIRONMENT=production
