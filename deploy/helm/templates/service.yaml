apiVersion: v1
kind: Service
metadata:
  name: {{ include "abra.fullname" . }}
  labels:
    {{- include "abra.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "abra.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: api
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
